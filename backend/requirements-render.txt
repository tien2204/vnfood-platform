# Bản cài cho deploy cloud (Render) — AI_BACKEND=http nên KHÔNG cần torch.
# torch/torchvision (~2GB) chỉ dùng khi AI_BACKEND=local (dev). Xem requirements.txt.

# Web framework
fastapi==0.111.0
starlette==0.37.2
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# Config
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# HTTP client (HttpPredictor gọi service AI trên HF)
httpx>=0.27.0,<1

# TTS (offline neural voice)
piper-tts>=1.4.2

# Ảnh — HttpPredictor encode JPEG bằng Pillow; numpy pin cho tương thích onnxruntime/piper
numpy<2
Pillow==10.3.0

# Utilities
python-slugify==8.0.4
email-validator==2.1.1

# Security
slowapi==0.1.9

# Infra clients
redis==5.2.1
boto3==1.35.99
