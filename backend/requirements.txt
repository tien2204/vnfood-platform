# Web framework
fastapi==0.111.0
starlette==0.37.2
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# Config
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# HTTP client
httpx>=0.27.0,<1

# TTS (offline, local neural voice — no network dependency)
piper-tts>=1.4.2

# AI / ML
# torch 2.2/2.3 CPU wheels are built against NumPy 1.x — pin <2 to avoid the
# "module compiled with NumPy 1.x cannot run in NumPy 2.x" crash at startup.
# (opencv-python declares numpy>=2 but is unused and still imports under 1.26.)
numpy<2
torch==2.3.0
torchvision==0.18.0
Pillow==10.3.0

# Utilities
python-slugify==8.0.4
email-validator==2.1.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
